<div>
  <h1 class='text-3xl mb-7'><%= t(".title") %></h2>

  <%= form_for @user do |form| %>
    <%= render partial: '/users/form', object: form %>

    <button class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded mb-7">
      Submit
    </button>

  <% end %>
</div>
