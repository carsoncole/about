%div.text-xl.md:text-2xl.font-bold
  %h2
    %span
      = succeed(", ") do
        = experience.title
    %span
      - if experience.url.present?
        %span.text-indigo-400
          = link_to experience.name, experience.url
      - else
        = experience.name


